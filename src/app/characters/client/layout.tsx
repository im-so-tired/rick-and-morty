export default function CharactersLayoutClients({
  children,
  modal
}: {
  children: React.ReactNode;
  modal: React.ReactNode;
}) {
  return (
    <>
      {children}
      {/*{modal}*/}
    </>
  );
}
