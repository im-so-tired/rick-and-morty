'use client';

import { List, Card, Grid, Input, Chip } from '@mantine/core';

export * from '@mantine/core';
export const ListItem = List.Item;
export const CardSection = Card.Section;
export const GridCol = Grid.Col;
export const InputLabel = Input.Label;
export const ChipGroup = Chip.Group;
